#splitting the data gained from the previous pipeline into training and testing batches:

from sklearn.model_selection import train_test_split

# X= features, y= masks
X = pipeline("insert_path")
y = pipeline("insert_path")

# making sure y is single channeled ( only has 1 dimension)
if y.shape[-1] > 1:
    y = y[:,:,:,0:1]

# splitting into training and validation data using a test size of 0.1:
X_train, X_val, y_train, y_val = train_test_split(
    X, y, test_size=0.1, random_state=42, shuffle=True
)

# splitting training further into training and testing data:
X_train, X_test, y_train, y_test = train_test_split(
    X_train, y_train, test_size=0.2, random_state=42, shuffle=True
)

print("Training samples:", X_train.shape[0])
print("Validation samples:", X_val.shape[0])
print("Testing samples:", X_test.shape[0])
